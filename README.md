# Hover-Card-Animation-Project
This project demonstrates a CSS hover animation effect applied to image cards. When the user hovers over each card, the background image shifts position and scales smoothly, creating a dynamic visual effect.
